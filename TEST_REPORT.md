# V0.7 强行为差异版测试报告

## 修改目标
针对 V0.6 中“不同烦恼虽然有类型，但实际点击和运动差异不够明显”的问题，本版只强化行为对象系统，不改整体叙事流程。

## 10 类对象的首轮可辨识行为
- B1 轻散型：轻微上浮；一次点击直接消散。
- B2 逃避型：靠近会躲；第一次点击会高速冲开，速度由普通约 20~50 提升到约 170~245+。
- B3 增殖型：正常删除时出现 3 个分裂残影；失控后真实分裂为 2~4 个。
- B4 回返型：正常阶段点击消失后约 1.05~1.55 秒从别处重新出现。
- B5 聚集型：持续向附近泡泡靠拢；点击时牵动最近 2~3 个泡泡。
- B6 顽固型：更重、更慢；正常阶段至少 2 次点击，失控阶段约 3 次并出现裂纹。
- B7 牵连型：与最近泡泡显示明显虚线连接；点击会让连接对象放大并弹开。
- B8 突发型：周期高速窜动；第一次点击速度可升至约 220~315。
- B9 压迫型：持续膨胀；第一次点击明显缩小但不会消失，继续处理才删除。
- B10 模糊型：默认明显失焦；第一次点击先“看清”，之后才能删除。

## 自动检查
1. `node --check js/*.js`：全部通过。
2. 数据映射检查：100 个预设、10 个行为类型、8 个内容分类，缺失行为映射为 0。
3. Node VM 行为测试：10 类对象首击事件均符合各自签名；逃避/突发速度跃迁、压迫缩小、模糊显现、顽固多击等状态均可被程序检测。
4. 保留 V0.6 的全屏 Canvas、渐红警戒、独裁者按钮、原地爆裂、重现、主动观察、退出重进清理逻辑。

## 环境说明
当前执行环境的 Chromium 对本地/localhost 页面有管理员策略拦截，因此未在此环境进行真实浏览器端端到端点击回归；已完成 JavaScript 语法检查和无浏览器依赖的行为状态测试。建议本地 Chrome/Edge 打开后重点试玩 10 类对象的首轮交互差异。
