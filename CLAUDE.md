# CLAUDE.md —— 项目长期规则

## 技术约束

- 使用原生 HTML / CSS / JavaScript 与 Canvas。
- 禁止新增后端、数据库、登录、云端保存、外部 AI/API、React/Vue 或多人联机。
- 优先适配桌面端 16:9：1366×768、1440×900、1920×1080。
- 阈值与时长统一放在 `js/config.js`，不得散落在业务代码中。

## 核心闭环

进入体验馆 → 输入烦恼 → 生成泡泡 → 正常删除 → 渐进失控 → 独裁者按钮显现
→ 连续清空 → 空白停顿 → 烦恼逐个重现 → 主题反思 → 返回首页

## 连续场景规则

- UX-07～UX-11 是独立后台节点，但必须共用 UX-07 的全屏视觉容器。
- UX-07→UX-08→UX-09→UX-10→UX-11 不得重新创建 Canvas 或进行整页淡出淡入。
- 临界值只在后台运算，不在前台展示任务条件或“还需点击几次”。
- 警戒感主要来自整个画面边缘的持续渐红和泡泡行为变化。
- 失控阶段的点击统计为 `deleteAttemptCount`，不得计入 `successfulDeleteCount`。
- 达到泡泡数量上限后仍应保持“无法真正删除”的叙事规则。

## 当前版本

- 当前版本：MVP V0.5。
- 已完成：全屏连续交互、chaosLevel、后台复合解锁、分裂/拒绝、连续清空、逐个重现。
- 下一阶段：真人试玩、参数调校、声音与高保真素材。

## 修改原则

- 修复问题时优先最小改动，不破坏完整闭环。
- 修改 UX-07～UX-11 前先检查共享视觉容器与场景映射。
- 新增动画必须支持 `prefers-reduced-motion`。
- 重新开始必须清除所有定时器、动画帧、泡泡和会话数据。
