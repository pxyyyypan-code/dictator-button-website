# CLAUDE.md —— 项目长期规则

## 技术约束

- 使用原生 HTML / CSS / JavaScript 与 Canvas。
- 禁止新增后端、数据库、登录、云端保存、外部 AI/API、React/Vue 或多人联机。
- 优先适配桌面端 16:9：1366×768、1440×900、1920×1080。
- 阈值与时长统一放在 `js/config.js`，不得散落在业务代码中。
- 字体统一走 `css/style.css` 的 `--ff` / `--ff-hand` 与 `js/font-support.js` 的
  `FontSupport.fontStackFor()`，不要在组件或绘制代码里另写 `font-family`。
- 改动任何 UI 文案后，必须重跑 `assets/dev/_build-fonts.py`，
  否则新字不在手写体子集里，会掉回默认字形。

## 核心闭环

进入体验馆 → 引导对话 → **选择一个烦恼** → **老虎机匹配道具** → 查看道具
→ 生成泡泡 → 正常删除 → 渐进失控 → 独裁者按钮显现 → 连续清空 → 空白停顿
→ 烦恼重现 → 个性化总结 → 体验记录

## 场景编号

V0.8 起使用 `u01`~`u12`，共 12 个节点。与 V0.7 的 `ux-01`~`ux-14` **不是整体平移**，
`u07` 起错开一位，任何 `s/ux-/u/` 式的批量替换都会把「失控」错送进「按钮页」：

| V0.8 | V0.7 | 内容 |
|---|---|---|
| u01 | ux-01 | 首页 |
| u02 | ux-02 + ux-03 | 引导对话 |
| u03 | ux-04 + ux-05 | 选择烦恼 |
| u04 | （全新） | 老虎机匹配 |
| u05 | （全新） | 匹配结果 |
| u06 | ux-06 + ux-07 | 平静删除 · **容器所有者** |
| u07 | ux-08 | 渐进失控 |
| u08 | ux-09 | 独裁者按钮 |
| u09 | ux-10 | 连续清空 / 空白 |
| u10 | ux-11 | 烦恼重现 |
| u11 | ux-12 | 个性化总结 |
| u12 | ux-13 + ux-14 | 体验记录 |

## 连续场景规则

- u06～u10 是独立后台节点，但 `viewId` 全部指向 `u06`，必须共用同一个全屏视觉容器。
- u06→u07→u08→u09→u10 不得重新创建 Canvas 或进行整页淡出淡入。
- `BubbleGame.init()` 只允许在 u06 调用一次；u07~u10 只能用 `setMode` /
  `setInteractive` / `startGrowth` / `settle` 等，再调 `init` 会清空泡泡、断掉连续性。
- `init()` 内部是 `mount(..., { interactive: false })`，调用后必须紧跟 `setInteractive(true)`。
- Canvas 及其祖先在 u06~u10 期间**不得 `display:none`**（挂载时尺寸 <2px 会让动画循环
  永久起不来，且完全静默）。需要隐藏时用 `opacity` / `filter`。
- Canvas 上不得加 `border` / `padding`，其祖先不得加 `transform`，否则点击坐标整体偏移。
- 临界值只在后台运算，不在前台展示任务条件或"还需点击几次"。
- 警戒感主要来自整个画面边缘的持续渐红和泡泡行为变化。
- 失控阶段的点击统计为 `deleteAttemptCount`，不得计入 `successfulDeleteCount`。
- 达到泡泡数量上限后仍应保持"无法真正删除"的叙事规则。

## 当前版本

- 当前版本：V0.8 开发中，按 `docs/V08_PLAN.md` 的七个阶段推进。
- 已完成：阶段 1 数据层（100 条烦恼 / 20 件道具 / 手写体子集）、阶段 2 骨架层（u01~u12）。
- 每个阶段完成后先与用户确认，确认后再推 GitHub，然后才进入下一阶段。

## 修改原则

- 修复问题时优先最小改动，不破坏完整闭环。
- 修改 u06～u10 前先检查共享视觉容器与场景映射。
- 所有 `data-bind` 节点必须首屏静态存在且此后不被重建——`bind()` 只做正向缓存、
  永不失效，节点被替换后写入会静默丢失。`data-bind` 名字必须全局唯一。
- 新增动画必须支持 `prefers-reduced-motion`。
- 重新开始必须清除所有定时器、动画帧、泡泡和会话数据。

## 行为对象系统

- `js/worry-data.js` 与 `js/gadget-data.js` 是生成产物，改内容要改数据源后重跑
  `assets/dev/_gen-data.py`，不要直接编辑。
- 用户自由输入必须一直可用，预设分类只提供灵感，不得改成必选问卷。
- 自由输入的分类置信度不足时必须请用户手选大类，**不得随机分配道具**。
- 前台不要显示 `B1~B10` 或"心理类型"标签；这些只属于互动实现。
- 正常阶段必须保持可控，但 10 类对象从一开始就应有清晰可辨的视觉与点击差异；
  transition/chaos 只负责继续放大这些差异。
- u10 的泡泡**不可点击**，只能看着它们回来；出口是面板上唯一的按钮，
  按下后直接进入 u11，不再有「再试一次 / 停下来看看」的二选一，也不再走 observe-select。
- 结尾只做记录，不做排行榜、不做评分、不做心理诊断结果。
- 继续维护前先读 `docs/AI_FLOW_SPEC.md` 与 `docs/V08_PLAN.md`。
